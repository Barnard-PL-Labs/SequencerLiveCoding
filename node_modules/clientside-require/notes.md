downfalls of sync_require
    - waiting until dependencies load
    - parsing files for dependencies



Components:
- caching
- async require in main page
- async AND sync in required js (modules and files)
- "pre-caching"
